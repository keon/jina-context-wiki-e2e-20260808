{
  "name": "@jina/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "eslint . --max-warnings 0",
    "test": "pnpm build && node --test dist/*.test.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@jina/ai": "workspace:*",
    "@jina/context-graph": "workspace:*",
    "@jina/daytona": "workspace:*",
    "@jina/observability": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1"
  }
}
