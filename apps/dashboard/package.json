{
  "name": "@jina/dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && node scripts/build-client.mjs",
    "typecheck": "tsc -b",
    "lint": "eslint . --max-warnings 0",
    "test": "pnpm build && node --test dist/app/*.test.js dist/server/*.test.js",
    "dev": "pnpm build && node dist/server/dev-server.js"
  },
  "dependencies": {
    "@cosmos.gl/graph": "^3.3.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "esbuild": "^0.25.12"
  }
}
