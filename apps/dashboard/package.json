{
  "name": "@jina/dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "echo \"lint not configured\"",
    "test": "echo \"test not configured\""
  },
  "dependencies": {
    "@jina/shared-kernel": "workspace:*"
  }
}
