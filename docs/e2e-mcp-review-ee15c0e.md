# MCP review release probe

This documentation-only marker exercises a workspace MCP-requested review on
the `ee15c0e` staging release. It intentionally does not alter a saved user-flow
Scenario so the caller's free-text review instructions remain the sole focus.
